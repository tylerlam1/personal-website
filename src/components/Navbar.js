import React from 'react';
import '../styles/Navbar.css'

const Navbar = () => {
    return (
        <nav className="navbar">
            <a href="#">Home</a>
        </nav>
    )
}

export default Navbar;